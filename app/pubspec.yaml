name: app
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  common_dependencies:
    path: '../core/common_dependencies'

  core_module:
    path: '../core/core_module'

  feature_dashboard:
    path: '../features/feature_dashboard'

  feature_currency_details:
    path: '../features/feature_currency_details'
  


dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.3.3

  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true

  assets:
    - .env
